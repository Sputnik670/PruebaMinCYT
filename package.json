{
  "name": "prueba-mincyt-root",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd frontend_dashboard && npm install",
    "dev:frontend": "cd frontend_dashboard && npm run dev",
    "dev:backend": "cd backend_dashboard && uvicorn main:app --reload"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}